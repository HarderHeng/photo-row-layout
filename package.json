{
  "name": "photo-row-layout",
  "version": "0.1.3",
  "description": "Lay images out the way a published site will: images written one after another in a paragraph become one equal-height row, with captions, optional EXIF, and grid / masonry / scroll blocks.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.mjs"
  }
}
